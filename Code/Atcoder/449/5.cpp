int solution(vector<vector<char>> grid) {
    int n = grid.size();
    int m = grid[0].size();
    int max_val = -1000000; // Initialize with a highly negative number

    auto is_digit = [](char c) {
        return c >= '0' && c <= '9';
    };

    //horizontally
    for (int i = 0; i < n; i++) {
        
    }
    
    return max_val;
}